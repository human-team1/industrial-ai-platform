# Graph

B 역할의 LangGraph 실험 코드를 두는 영역입니다.

예상 파일:

- `graph_v0.py`
- `source_verifier.py`
- `schemas.py`

정식 서비스 코드로 승격할 때는 `application/`, `domain/`, `infrastructure/`, `container/`, `api/` 계층으로 나눠 반영합니다.
