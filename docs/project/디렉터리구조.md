backend-spring/
└── src/
    └── main/
        ├── java/com/example/factoryguard/
        │
        │   ├── common/
        │   │   ├── exception/              // 전역 예외, 커스텀 예외, 에러코드
        │   │   ├── response/               // 공통 응답 포맷(ApiResponse 등)
        │   │   └── util/                   // 날짜, 문자열, 파일명 등 공통 유틸
        │   │
        │   ├── config/
        │   │   ├── security/               // Spring Security, JWT, 인증/인가 설정
        │   │   ├── jpa/                    // JPA Auditing, QueryDSL 등 설정
        │   │   ├── web/                    // CORS, WebMvc, 인터셉터 설정
        │   │   └── client/                 // FastAPI 호출용 WebClient/RestClient 설정
        │   │
        │   ├── domain/
        │   │   ├── inspection/
        │   │   │   ├── model/              // 검사 요청, 검사 대상, 검사 상태 도메인 모델
        │   │   │   ├── vo/                 // InspectionId, Category 같은 값 객체
        │   │   │   └── exception/          // 검사 도메인 예외
        │   │   │
        │   │   ├── result/
        │   │   │   ├── model/              // anomaly score, 판정 결과, 재검토 상태 모델
        │   │   │   ├── vo/                 // Score, HeatmapPath 등 값 객체
        │   │   │   └── exception/          // 결과 도메인 예외
        │   │   │
        │   │   ├── user/
        │   │   │   ├── model/              // 사용자, 권한, 관리자 여부
        │   │   │   └── exception/          // 사용자 도메인 예외
        │   │   │
        │   │   ├── knowledge/
        │   │   │   └── model/              // 문서 질의 이력, 답변 이력 등 최소 도메인
        │   │   │
        │   │   └── dashboard/
        │   │       └── model/              // 통계 조회용 집계 모델
        │   │
        │   ├── application/
        │   │   ├── port/
        │   │   │   ├── in/
        │   │   │   │   ├── inspection/     // 외부에서 호출하는 검사 유스케이스
        │   │   │   │   ├── result/         // 결과 조회/재검토 유스케이스
        │   │   │   │   ├── knowledge/      // RAG 질의 유스케이스
        │   │   │   │   ├── auth/           // 로그인/토큰 유스케이스
        │   │   │   │   └── dashboard/      // 통계/대시보드 유스케이스
        │   │   │   │
        │   │   │   └── out/
        │   │   │       ├── inspection/     // 검사 저장/조회 포트
        │   │   │       ├── result/         // 결과 저장/조회 포트
        │   │   │       ├── user/           // 사용자 조회/저장 포트
        │   │   │       ├── ai/             // FastAPI AI 추론 호출 포트
        │   │   │       ├── knowledge/      // FastAPI RAG 호출 포트
        │   │   │       └── dashboard/      // 통계 조회 포트
        │   │   │
        │   │   ├── service/
        │   │   │   ├── inspection/         // 검사 요청/상세/목록 유스케이스 구현
        │   │   │   ├── result/             // 결과 저장/조회/재검토 구현
        │   │   │   ├── knowledge/          // 문서 질의/답변 이력 구현
        │   │   │   ├── auth/               // 로그인/인증 구현
        │   │   │   └── dashboard/          // 통계/대시보드 구현
        │   │   │
        │   │   └── dto/
        │   │       ├── inspection/         // 유스케이스 command/result DTO
        │   │       ├── result/             // 결과 DTO
        │   │       ├── knowledge/          // 질의/응답 DTO
        │   │       ├── auth/               // 인증 DTO
        │   │       └── dashboard/          // 통계 DTO
        │   │
        │   ├── adapter/
        │   │   ├── in/
        │   │   │   └── web/
        │   │   │       ├── inspection/     // 검사 요청/조회 Controller
        │   │   │       ├── result/         // 결과 조회/재검토 Controller
        │   │   │       ├── knowledge/      // 챗봇/RAG 질의 Controller
        │   │   │       ├── auth/           // 로그인/토큰 Controller
        │   │   │       └── dashboard/      // 대시보드/통계 Controller
        │   │   │
        │   │   └── out/
        │   │       ├── persistence/
        │   │       │   ├── inspection/     // 검사 JPA Entity, Repository, Adapter
        │   │       │   ├── result/         // 결과 JPA Entity, Repository, Adapter
        │   │       │   ├── user/           // 사용자 JPA 구현체
        │   │       │   ├── knowledge/      // 질의 이력/답변 이력 저장 구현체
        │   │       │   └── dashboard/      // 통계 쿼리 구현체
        │   │       │
        │   │       └── fastapi/
        │   │           ├── client/         // FastAPI 호출 클라이언트
        │   │           ├── request/        // FastAPI 요청 DTO
        │   │           ├── response/       // FastAPI 응답 DTO
        │   │           ├── ai/             // 이상탐지 추론 Port 구현체
        │   │           └── knowledge/      // RAG 질의 Port 구현체
        │   │
        │   └── FactoryGuardApplication.java // Spring Boot 시작점
        │
        └── resources/
            ├── application.yml              // 공통 설정
            ├── application-local.yml        // 로컬 환경
            ├── application-dev.yml          // 개발 환경
            └── application-prod.yml         // 운영 환경

src/
├── app/                // 앱 초기화, 라우팅, 전역 Provider, 공통 설정
├── pages/              // URL 라우트에 대응하는 페이지 컴포넌트
├── widgets/            // 여러 feature/entity를 조합한 화면 단위 UI 블록
├── features/           // 사용자 행동/기능 단위 모듈
│   ├── inspection/
│   │   ├── api/        // 검사 기능 전용 API 요청/응답 처리
│   │   ├── ui/         // 검사 기능에 속한 UI 컴포넌트
│   │   ├── model/      // 상태 관리, 훅, 비즈니스 로직
│   │   └── types/      // 검사 기능 전용 타입 정의
│   ├── result/
│   ├── chatbot/
│   └── auth/
├── entities/           // 핵심 도메인 데이터 모델과 엔티티별 로직
└── shared/             // 전역에서 재사용하는 공통 모듈
    ├── api/            // 공통 API 클라이언트, 인터셉터, 유틸
    ├── ui/             // 공통 UI 컴포넌트
    ├── hooks/          // 공통 커스텀 훅
    ├── lib/            // 범용 유틸리티 함수 및 라이브러리 래퍼
    └── types/          // 공통 타입 정의

ai-server/
├── api/                 // FastAPI 라우터
├── application/         // 추론 유스케이스, RAG 유스케이스
├── domain/              // 추론 결과 모델, 공통 인터페이스
├── infrastructure/      // 모델 로더, 벡터DB, 스토리지, 캐시
├── container/           // DI 컨테이너, 의존성 조립
├── config/              // 환경설정
└── main.py              // 앱 시작점
